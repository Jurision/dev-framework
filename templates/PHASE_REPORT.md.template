# Phase report — <phase / sub-phase>

The agent reports these three; the owner **accepts before the next sub-phase** (standards/11
§6). No code-reading required — answers in plain language, claims **git-verifiable**
(`standards/08` §5).

## 1. Done vs not done (against the implementation plan)
- **Done:** <items, citing the plan's steps>
- **Not done / deferred:** <items + why>

## 2. What changed
- **Changed (files / behavior):** <list — citable by commit hash>
- **Omissions:** <none / …>
- **Over-doing (anything not in this phase's scope):** <none / …>

## 3. Verification & next
- **Verified by:** <command / test / observable> → **result:** <pass / fail + evidence>
- **Next sub-phase:** <which> — **why:** <reason; must match the plan — if it doesn't, that's
  drift, pull back>
