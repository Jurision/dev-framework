# Phase report — <phase / sub-phase>

The agent's report is **input to acceptance, not acceptance itself** (standards/11 §6). It
helps **product acceptance** (plain language, no code-reading); it does **not** replace
**technical verification** (independent review of the diff/tests/security/running evidence).

## Reported by
<agent / implementer>

## Record
- **Plan revision:** <which implementation plan + version>
- **PR / commit range:** <#PR · sha..sha — git-verifiable, `standards/08` §5>
- **Changed files:** <list>

## Evidence
- **CI run:** <link / status>
- **Test output:** <fail-before/pass-after for fixes; results>
- **Runtime proof:** <screenshot / endpoint / observable effect — "200 ≠ healthy", `05`>
- **Known risks:** <…>

## Deviations (against the plan)
- **Omitted:** <…>
- **Added outside scope:** <…>
- **Open issues:** <…>

## Decisions
- **Product acceptance:** Accepted | Rejected | Needs changes — **by:** <owner>
- **Technical verification:** Passed | Failed | Pending — **by:** <reviewer>

> Acceptance proceeds to the next checkpoint only when **both** are satisfied
> (`standards/00` two roles; claimed ≠ done).
