# Feature spec: <feature>

The contract for **one feature** (standards/11 §3). **Owns:** scope, behavior, business rules,
non-goals, acceptance. *What* the product wants — not *how* to build it (that's the
implementation plan) or *why it was chosen* (that's a decision record).

- **Source:** <link to PROJECT_BRIEF + version/commit it derives from>

## Goal
<the user or system outcome this feature delivers>

## Actors
- <who / what triggers and uses it>

## Preconditions
- <state that must hold before this applies>

## Behavior & business rules
- <the rules — inputs → outputs, ordering, calculations, constraints>

## Edge & failure cases
- <empty / invalid / conflicting / offline / over-limit → expected behavior>

## Permission & data impact
- <who may do it; what data is read/written; sensitivity — `standards/07`>

## Non-functional requirements
- <performance / a11y / latency / limits that apply — link the relevant standard/modifier>

## Observable result (acceptance)
- <what an accepter can see/verify that proves it works — the "done =" condition>

## Non-goals
- <explicitly out of scope for this feature>

## Dependencies & open questions
- <other features / decisions this needs; anything unresolved>
